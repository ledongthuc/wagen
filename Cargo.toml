[package]
name = "wavegen"
version = "0.1.0"
edition = "2024"

[dependencies]
symphonia = { version = "0.6", features = ["mp3", "wav", "flac", "ogg", "pcm", "mkv"] }
anyhow = "1"
clap = { version = "4.6.1", features = ["derive"] }
